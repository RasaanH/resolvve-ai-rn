import { View } from "react-native";
import { Text } from "react-native-paper";

export default function Subscription() {
  return (
    <View>
      <Text>You are now on the subscriptions page</Text>
    </View>
  );
}
